# デプロイ手順書

業務用エアコン修理比較サイトのデプロイ手順です。

---

## 📋 前提条件

- **ドメイン**: aircon-gekiyasuou.jp
- **デプロイ先**: Cloudflare Pages（推奨）
- **または**: FTP、Git、rsync

---

## 🚀 方法0: Cloudflare Pagesへのデプロイ（推奨）

### 利点
- ✅ **無料で使用可能**
- ✅ **グローバルCDN**（高速配信）
- ✅ **自動HTTPS**（SSL証明書不要）
- ✅ **Git連携で自動デプロイ**
- ✅ **プレビュー環境**自動生成
- ✅ **Rollback機能**（ワンクリックで元に戻せる）

### 手順

#### A. GitHubにリポジトリを作成

1. **GitHubで新しいリポジトリを作成**
   ```bash
   # リポジトリ名: gyomuyou-aircon-hikaku
   # パブリック or プライベートを選択
   ```

2. **ローカルでGit初期化**
   ```bash
   cd /Users/sales/genki-denki/dev/gyomuyou-aircon-hikaku

   # .gitignoreを作成
   echo ".DS_Store" > .gitignore
   echo "node_modules/" >> .gitignore

   # Git初期化
   git init
   git add .
   git commit -m "Initial commit: 比較サイト完成"
   ```

3. **GitHubにプッシュ**
   ```bash
   # リモートリポジトリを追加
   git remote add origin https://github.com/your-username/gyomuyou-aircon-hikaku.git

   # ブランチ名をmainに変更（GitHubのデフォルト）
   git branch -M main

   # プッシュ
   git push -u origin main
   ```

#### B. Cloudflare Pagesに連携

1. **Cloudflareダッシュボードにログイン**
   https://dash.cloudflare.com/

2. **Pagesを作成**
   - 左側メニュー → **Pages**
   - **Create a project** をクリック
   - **Connect to Git** を選択

3. **GitHub連携**
   - **Connect GitHub** をクリック
   - リポジトリ `gyomuyou-aircon-hikaku` を選択
   - **Begin setup** をクリック

4. **ビルド設定**
   ```yaml
   # Framework preset: None
   # Build command: （空のまま）
   # Build output directory: /
   ```

5. **環境変数（必要な場合）**
   - 特になし（静的サイトのため）

6. **デプロイ**
   - **Save and Deploy** をクリック
   - 自動的にビルドが開始されます

#### C. カスタムドメインの設定

1. **ドメインを追加**
   - Cloudflare Pages ダッシュボード
   - **Custom domains** → **Set up a custom domain**
   - `aircon-gekiyasuou.jp` を入力

2. **DNS設定**
   - Cloudflareが自動でDNSレコードを追加
   - ドメインのネームサーバーをCloudflareに変更:
     ```
     ns1.cloudflare.com
     ns2.cloudflare.com
     ```

3. **SSL証明書**
   - Cloudflareが自動で発行・設定
   - **Always HTTPS** を有効化（推奨）

### 更新手順

```bash
# 1. ファイルを編集
code data/companies.json

# 2. コミット
git add .
git commit -m "Update: 企業データを更新"

# 3. プッシュ
git push

# 自動でCloudflare Pagesにデプロイされます（約1〜2分）
```

### プレビュー環境

各ブランチごとに自動的にプレビューURLが生成されます：
- `main` ブランチ: `https://gyomuyou-aircon-hikaku.pages.dev`
- その他のブランチ: `https://branch-name.gyomuyou-aircon-hikaku.pages.dev`

---

## 📁 方法1: FTPでのデプロイ

### 手順

1. **FTPクライアントで接続**
   - FileZilla、Cyberduck、またはTransmitなどのFTPクライアントを起動
   - サーバー情報を入力して接続
     - ホスト: aircon-gekiyasuou.jp（またはFTPサーバーアドレス）
     - ユーザー名: （ご自身のFTPユーザー名）
     - パスワード: （ご自身のFTPパスワード）

2. **ファイルをアップロード**

   プロジェクトのファイルを以下の構造でアップロードします：

   ```
   /public_html/ または /www/
   ├── index.html
   ├── robots.txt
   ├── sitemap.xml
   ├── css/
   │   └── style.css
   ├── js/
   │   └── main.js
   ├── data/
   │   └── companies.json
   ├── pages/
   │   ├── genki-denki.html
   │   ├── aircon-pro-repair.html
   │   ├── rockfield.html
   │   ├── aircon-honten.html
   │   ├── shuri110.html
   │   └── fuji-setubi.html
   ├── articles/
   │   ├── aircon-repair-methods.html
   │   ├── aircon-failure-causes.html
   │   ├── repair-vs-replacement.html
   │   ├── aircon-repair-cost.html
   │   ├── how-to-choose-repair-company.html
   │   └── emergency-response-guide.html
   └── images/
       └── （ロゴ・アイコン等の画像ファイル）
   ```

3. **パーミッション設定**

   以下のファイルに適切なパーミッションを設定：

   - HTMLファイル: 644
   - CSS/JSファイル: 644
   - JSONファイル: 644
   - ディレクトリ: 755

   FileZillaの場合:
   - ファイルを右クリック → 「ファイルのアクセス権限」→ 数値を入力

4. **動作確認**

   - https://aircon-gekiyasuou.jp/ にアクセス
   - ランディングページが正しく表示されるか確認
   - 比較表、FAQ、詳細ページへのリンクが動作するか確認
   - モバイル表示（レスポンシブ）を確認

---

## 方法2: Gitでのデプロイ

### 手順

1. **Gitリポジトリの初期化（まだの場合）**

   ```bash
   cd /path/to/gyomuyou-aircon-hikaku
   git init
   git add .
   git commit -m "Initial commit: 比較サイト完成"
   ```

2. **リモートリポジトリの追加**

   ```bash
   # GitHubの場合
   git remote add origin https://github.com/your-username/gyomuyou-aircon-hikaku.git

   # またはGitLabの場合
   git remote add origin https://gitlab.com/your-username/gyomuyou-aircon-hikaku.git
   ```

3. **プッシュ**

   ```bash
   git push -u origin main
   ```

4. **サーバーでのデプロイ**

   方法A: **GitHub Pages**
   ```bash
   # リポジトリ設定でGitHub Pagesを有効化
   # Settings → Pages → Source: main branch
   # URL: https://your-username.github.io/gyomuyou-aircon-hikaku/
   ```

   方法B: **サーバーでgit pull**
   ```bash
   # サーバーにSSH接続
   ssh user@aircon-gekiyasuou.jp

   # ドキュメントルートへ移動
   cd /var/www/html

   # リポジトリをクローン（初回のみ）
   git clone https://github.com/your-username/gyomuyou-aircon-hikaku.git .

   # 2回目以降はpull
   git pull origin main
   ```

---

## 方法3: rsyncでのデプロイ（推奨）

高速で安全な転送方法です。

```bash
# ローカルからサーバーへ転送
rsync -avz --delete \
  /Users/sales/genki-denki/dev/gyomuyou-aircon-hikaku/ \
  user@aircon-gekiyasuou.jp:/var/www/html/

# オプション説明:
# -a: アーカイブモード（パーミッション・タイムスタンプ保持）
# -v: 詳細表示
# -z: 圧縮転送
# --delete: ローカルにないファイルを削除（同期）
```

---

## 🔍 デプロイ後のチェックリスト

- [ ] トップページが正しく表示される
- [ ] CSSが適用されている（カラー#EB6601）
- [ ] 比較表が正しく表示される
- [ ] フィルター機能が動作する
- [ ] FAQアコーディオンが動作する
- [ ] 企業別詳細ページへのリンクが動作する
- [ ] レスポンシブデザイン（モバイル表示）が正しい
- [ ] robots.txtにアクセスできる（https://aircon-gekiyasuou.jp/robots.txt）
- [ ] sitemap.xmlにアクセスできる（https://aircon-gekiyasuou.jp/sitemap.xml）

---

## 🚀 Google Search Consoleに登録

1. **Google Search Consoleにアクセス**
   https://search.google.com/search-console

2. **プロパティを追加**
   - 「URLプレフィックス」を選択
   - `https://aircon-gekiyasuou.jp/` を入力

3. **サイトマップの送信**
   - 左側メニュー → 「サイトマップ」
   - `sitemap.xml` を送信

4. **インデックス登録リクエスト**
   - URL検査 → 「インデックス登録をリクエスト」

---

## 🔄 定期更新方法

### 企業データの更新

1. `data/companies.json` を編集
2. 変更を反映してデプロイ
3. Google Search Consoleでサイトマップを再送信

### 新規記事の追加

1. `articles/` に新しいHTMLファイルを作成
2. `sitemap.xml` にURLを追加
3. `index.html` の記事セクションにリンクを追加
4. デプロイ

---

## 🐥 トラブルシューティング

### サイトが表示されない

- **原因**: ファイルがアップロードされていない
- **解決**: FTPクライアントで転送ログを確認

### CSSが適用されない

- **原因**: パスが間違っている
- **解決**: `<link rel="stylesheet" href="css/style.css">` のパスを確認

### JavaScriptが動作しない

- **原因**: ファイルパスが間違っている
- **解決**: `<script src="js/main.js"></script>` のパスを確認

### 404エラー

- **原因**: ファイル名の大文字・小文字の違い（Linuxサーバーは大文字小文字を区別）
- **解決**: ファイル名が正しいか確認（例: genki-denki.html vs Genki-Denki.html）

---

## 📞 サポート

不明点があれば、以下のリソースを参照してください：

- **Webサーバー**: サーバー管理会社のマニュアル
- **FTPツール**: FileZillaドキュメント https://wiki.filezilla-project.org/Documentation
- **Git**: Pro Git Book https://git-scm.com/book/ja/v2

---

**最終更新**: 2026-01-28
